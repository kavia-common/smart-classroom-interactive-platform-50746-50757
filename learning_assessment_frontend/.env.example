# Smart TV Learning Assessment Frontend

# Base URL of the backend API (Django). The frontend will use stubbed responses
# if the backend is not available.
REACT_APP_API_BASE_URL=http://localhost:3001
